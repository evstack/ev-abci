# Minimal base image for integration tests
FROM debian:bullseye-slim AS binaries

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates \
    curl \
    jq \
    bash \
    coreutils \
    netcat-openbsd \
    openssl \
    wget \
    git \
  && rm -rf /var/lib/apt/lists/*

# This image intentionally does not compile chain binaries yet.
# It serves as a shared base layer for later test containers.

# Show versions for debugging
RUN echo "curl:" $(curl --version | head -n1) && \
    echo "jq:" $(jq --version)

